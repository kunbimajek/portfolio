import '../App.sass';

import Socials from '../components/Socials';

const Footer = () => {
    return (
        <footer>
            <nav className="container-fluid">
                <Socials fill="#ffffff"/>
            </nav>
        </footer>
    )
}

export default Footer;